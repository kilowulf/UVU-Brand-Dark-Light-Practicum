{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "json-server db.json --routes routes.json",
    "reset-db": "cp db.bak.json db.json"
  },
  "devDependencies": {
    "json-server": "0.17.0"
  }
}
